///scr_dialogue_default(tree)

//Runs the user_defined_2 event on given tree to restore the tree to default.

//  tree            = The tree which is to be ended.

//Converts arguments to locals.
var tree = argument0;

//Runs the user_defined event.
user_event(tree, 2);
